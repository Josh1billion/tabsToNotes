var config = {};

config.urlRoot    = 'http://localhost:3000'; // if the user will see the port while browsing, you must include it here, otherwise don't
config.port       = 3000;

module.exports = config;
